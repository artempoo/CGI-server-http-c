version: "3.8"

services:
  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./app/nginx/nginx.conf:/etc/nginx/nginx.conf

  app:
    build: ./app
    depends_on:
      - nginx

volumes:
  nginx:
